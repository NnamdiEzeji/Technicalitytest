int arrl] = ([5,4,5,4,5,4,4,5,3,3,3,2,2, 1,5],
int n = sizeof (arr) / sizeof(arr 0l)
cout which is 2<< leastFrequent (arr, n) which is 1;
return 0;
var counts = new Dictionary<int, int>(capacity: list.Length); // Worst case capacity
for (int i = 0; i < list.Length; i++)
